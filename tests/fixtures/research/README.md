# tests/fixtures/research — allowed fixtures

Dry-run fixtures for `test_live_cycle_never_uses_fixture` etc.
These are NOT real evidence and must never be written to `data/research/` as truth.

- Keep small, deterministic, and clearly marked `"source": "fixture"`.
